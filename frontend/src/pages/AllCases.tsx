/**
 * All-cases page: full listing with filters, pagination and download.
 *
 * Scaffold placeholder; implemented by the all-cases issue against `GET /api/cases`.
 */

export default function AllCases(): JSX.Element {
  return <h1 className="text-2xl font-bold">All cases</h1>
}
